

<header class="row ">
    <div class="col ">
      <h1 class="display-3 d-flex justify-content-center">Eat-Da-Burgers</h1>
      <p class="d-flex justify-content-center">
      <img src="/assets/img/burger.png" alt="burger" height="200"></p>
      <p class = "d-flex justify-content-center">  Devour a burger from left column or add new one using form </p>
    </div>
</header>

<main class="container">
<div class="row justify-content-md-center">
<div class="col-md-4">
<h2>Burgers to eat!</h2>

<ul>
  {{#each burgers}}
    {{#unless devoured}}
      <li class = "p-1 m-1">
	    

	    <button class="devour btn btn-primary btn-sm" data-id="{{id}}" >
		DEVOUR <i class="fas fa-utensils"></i>
	    </button>
        {{burger_name}}
        </li>
    {{/unless}}
  {{/each}}
</ul>
</div>

<div class="col-md-4">
<h2>Devoured burgers</h2>

<ul>
  {{#each burgers}}
    {{#if devoured}}
      <li class = "p-1 m-1">
          {{burger_name}}
      </li>
    {{/if}}
  {{/each}}
</ul>
</div>
</div> 
<div class="row justify-content-md-center">
<div class="col-md-4">
<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="bu">Burger Name:</label>
    <input type="text" id="bu" name="name">
  </div>

  <button type="submit" class = "btn btn-primary">Add <i class="fas fa-hamburger"></i></button>
</form>
</div>
</div>
</main>
